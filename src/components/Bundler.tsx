function Bundler() {
  /*
    TODO 8/29/2025:
    The concept of "bundling" will be allowing the user to select multiple elements, which will then be bundled into a group
    That bundle will be usable in future resumes

    Have to consider whether the bundle will be a snapshot or just hold references - probably references
    */
  return <div></div>;
}

export default Bundler;
